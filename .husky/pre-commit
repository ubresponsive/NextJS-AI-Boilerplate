echo "🔍 Running pre-commit checks..."

# Format code (non-blocking)
echo "🎨 Formatting code..."
npm run format

# Basic lint check (non-blocking warnings)
echo "🔧 Checking for lint issues..."
npm run lint || echo "⚠️  Lint warnings found, but commit will proceed"

echo "✅ Pre-commit checks completed!"
